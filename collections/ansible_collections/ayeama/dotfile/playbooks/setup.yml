- hosts: all
  gather_facts: true
  tasks:
    - name: gnome
      ansible.builtin.include_role:
        name: ayeama.dotfile.gnome
      when: ansible_env.XDG_CURRENT_DESKTOP == "GNOME"

    - name: vim
      ansible.builtin.include_role:
        name: ayeama.dotfile.vim
